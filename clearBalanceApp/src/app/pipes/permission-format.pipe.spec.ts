import { PermissionFormatPipe } from './permission-format.pipe';

describe('PermissionFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new PermissionFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
